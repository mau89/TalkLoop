---
description: Обязательная доставка каждого завершённого изменения через новую ветку, коммит и push
alwaysApply: true
---

# Git delivery workflow

- Перед созданием ветки проверяй историю имён и следуй соглашению репозитория.
- Для ежедневных заданий используй формат `🔥-День-N.-Краткое-название`, например
  `🔥-День-14.-Инварианты-и-ограничения-состояния`.
- Не переиспользуй ветку предыдущей задачи, если пользователь явно этого не попросил.
- Перед коммитом запускай релевантные тесты и `git diff --check`.
- Добавляй в коммит только изменения текущей задачи, не затрагивая чужую незавершённую работу.
- Создавай содержательный коммит и отправляй ветку командой `git push -u origin <branch>`.
- В результате сообщай имя ветки, хеш коммита и статус проверок.
- Не выполняй commit или push только если пользователь явно попросил этого не делать.
